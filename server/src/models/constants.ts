export const TABLES ={
    QUESTIONS: 'questions',
    CATEGORIES: 'categories',
    CHOICES: 'choices',
} as const;

export const QUIZ_SETTINGS = {
    DEFAULT_QUESTION_COUNT: 10,
    DEFAULT_QUESTION_TIMER: 30,
} as const;